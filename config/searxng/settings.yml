use_default_settings: true

server:
  bind_address: 0.0.0.0
  port: 8080
  secret_key: "replace-me-with-a-long-local-secret"
  limiter: false
  image_proxy: true

search:
  safe_search: 1
  autocomplete: ""
  default_lang: "en"

ui:
  static_use_hash: true

